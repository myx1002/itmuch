# 应用服务 WEB 访问端口
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_center?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 941002
    driver-class-name: com.mysql.cj.jdbc.Driver

#下面这些内容是为了让MyBatis映射
mybatis:
  #指定Mybatis的Mapper文件
  mapper-locations:
    classpath:
      mappers/*xml
  #指定Mybatis的实体目录
  type-aliases-package:
    com.itmuch.usercenter.mybatis.entity

